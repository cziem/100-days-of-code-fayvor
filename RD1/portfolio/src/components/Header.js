const Header = document.querySelector('.container__header');

// Get HeaderBackgroundImages
const HeaderBackgroundImages = [
  'bng1', 'bng2', 'bng3', 'bng4', 'bng5'
]

export { Header, HeaderBackgroundImages };